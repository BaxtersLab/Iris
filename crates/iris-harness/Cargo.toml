[package]
name = "iris-harness"
version = "0.1.0"
edition = "2021"
description = "Test-stream harness: MPEG-TS via ffmpeg or deterministic JSONL telemetry"

[dependencies]
iris-ipc = { path = "../iris-ipc" }
serde_json = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tempfile = "3.3"
